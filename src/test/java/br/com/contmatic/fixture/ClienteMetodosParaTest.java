package br.com.contmatic.fixture;

public class ClienteMetodosParaTest {


//
//    public Set<String> validador(String argumento) {
//        Cliente cliente = cliente(argumento);
//        Validator validador = Validation.buildDefaultValidatorFactory().getValidator();
//        Set<ConstraintViolation<Cliente>> erros = validador.validate(cliente);
//        Set<String> erros2 = new TreeSet<String>();
//        erros.stream().forEach(erro -> erros2.add(erro.getMessage()));
//        Preconditions.checkArgument(erros.size() <= 0, new IllegalAccessError(erros2.toString()));
//        return erros2;
//
//    }
//
    public static void main(String[] args) {
//        for(int i = 0 ; i <= 6 ; i++) {
//            Cliente cliente = cliente("validos");
//            MongoClient dadosDoCli = new MongoClient("localhost");
//            MongoDatabase database = dadosDoCli.getDatabase("Prova3");
//            MongoCollection<Document> clientes = database.getCollection("Cliente");
//            clientes.insertOne(new Document("_id", cliente.getCpf()).append("nome", cliente.getNome()).append("email", cliente.getEmail()));
//       System.out.println(emailValido());
        }
//    }
}
